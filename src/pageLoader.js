import { URL } from 'url'
import axios from 'axios'
import fsp from 'fs/promises'

const loader = (url) => {
  // const fileName
  // axios.get(url).then(response => fsp.copyFile(response.data, './tmp/1'))
  // fsp.readFile('./tmp/1', { encoding: 'utf8' }).then(console.log)
}
export default loader
